import 'package:flutter/material.dart';

class WidgetWeatherAppImage extends StatelessWidget {
  const WidgetWeatherAppImage({super.key});

  @override
  Widget build(BuildContext context) {
    return const FlutterLogo(
      size: 34,
    );
  }
}
